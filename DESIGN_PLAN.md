# MyTerra — UX/UI design plan

**Scope:** PWA design only. No native, no App Store, no monetization mechanics. Every change ships through `src/App.jsx`, `index.html`, CSS, and Vercel auto-deploy.

**Status legend:** `[ ]` not started · `[~]` in progress · `[x]` done

> **2026-05-21 recovery note.** This file was accidentally deleted in commit `bf92dee` ("Clean up codecheck findings", 2026-05-20) — an automated codecheck tool flagged it as unused doc and the cleanup was applied too aggressively. Restored from `bf92dee^:DESIGN_PLAN.md` and audited against shipped code. The audit pass also corrected three stale entries: **8.2 + 8.3** (framer-motion was torn out in commit `1db4515` on 2026-05-19 — replaced with native CSS), **6.9.6** (4 of 10 zone tiles ship, not 10), and a new **6.10** section captures the Living Farm Map polish work that happened between the 2026-05-15 plan freeze and the 2026-05-20 accidental deletion.


---

## North star

**Mood sentence:** *A calm, modern coach that helps anxious millennials grow their first plants without feeling stupid.*

**Design lane:** Notion × Planta hybrid.

- **From Notion:** calm density, sidebar IA, quiet metadata pills, restrained color (one accent on neutral), proper typography hierarchy, no decorative noise.
- **From Planta:** warm palette, conversational microcopy, plant-as-companion framing, illustrated empty states, gentle care patterns.
- **Arbitration rule when they conflict:** headers, copy, and empty states lean Planta. Lists, detail views, and dense data lean Notion. Cream backgrounds carry the warmth, forest green carries the accent restraint.

**Reference apps to keep open while working:** Planta (visual + tone), Things 3 (task interaction), Apple Fitness (stat visualization), Notion mobile (IA on small screens).

---

## Locked decisions (2026-05-07)

1. Lane: Notion × Planta hybrid.
2. Palette: keep cream + forest green, refine the exact tones.
3. Icons: Lucide React + Twemoji-as-SVG bridge for now. Custom illustrated set is a later commission.
4. This file is the working plan. Update statuses as we ship.

---

## Phase 1 — Foundations (must precede pixel work)

- [x] **1.1 Tokenize the design.** `:root` block added to `index.css` — all colors, shadows, radius, spacing, type scale as CSS vars. `theme.js` C values now reference `var(--...)`. Dark-mode ready.
- [x] **1.2 Refine cream + forest tones.** `--color-bg: #f7f6f3` (warm neutral, not orange). `--color-green: #2e6b52` (more sage). `--color-green-light: #3d9970`. Font import trimmed to 400+600 only.
- [x] **1.3 Type scale.** `--text-xs/sm/base/md/lg/xl` vars (12/14/16/18/22/28). `TS` export added to `theme.js`. Body font-size set to `var(--text-base)` in CSS.
- [x] **1.4 Component library audit.** Components: `Btn` (6 variants: primary/secondary/danger/ghost/success/orange), `Card`, `Inp`, `Sel`, `Txt`, `Overlay`, `Pill`, `Tooltip`, `Ring`, `Stat`. No duplicate definitions. Fixed 3 raw `<button>` elements in FeedbackSurvey/FeedbackPrompt that bypassed `<Btn>`. **Remaining for Phase 3.5:** ~15 inline `<span>` pills throughout that need `<Pill tone="...">` consolidation.

## Phase 2 — Mobile-responsive PWA

The current `@media (width <= 700px)` block contains 3 rules. This is the single largest gap.

- [x] **2.1 Real breakpoints.** 0–479 phone, 480–767 phablet, 768–1023 tablet, 1024+ desktop. Each gets a dedicated layout.
- [x] **2.2 Bottom-tab navigation on mobile.** Five tabs: **Today / Farm / Animals / Pantry / More.** "More" holds Calendar, Financials, Manuals, Settings. Sidebar collapses to this on phone, becomes a rail on tablet, stays as today on desktop.
- [x] **2.3 Stack-first layout on phone.** All grids collapse to single column. `.g2/.g3/.g5` utility classes in CSS; 6 fixed grids in App.jsx converted.
- [x] **2.4 Bottom sheets for detail views.** Overlay component slides up from bottom on phone. `.overlay-backdrop/.overlay-sheet/.overlay-handle-row` CSS classes added.
- [~] **2.5 Swipe gestures on lists.** Swipe-right to mark done shipped on Today's task pipeline + TaskQueue rows (step tasks toggle their step, regular tasks call `markTaskDone`). Swipe-left to remove shipped on Pantry items (calls `del(item.id)` with a red "🗑 Remove" reveal). New `useSwipe` hook (`src/lib/use-swipe.js`) is touch-only — desktop click handlers are unaffected. New `SwipeableRow` wrapper (`src/components/ui.jsx`) holds the reveal background + transform. Animals deliberately skipped — they have no clean delete action (only quantity-based slaughter). Pull-to-refresh deferred to post-Supabase (Phase 5) — refresh has no meaningful action in an offline-first app today, shipping a dead lever would be worse than skipping it.
- [x] **2.6 Touch targets 44px buttons / 56px+ rows and gesture areas.** Close, calendar nav, AI send all 44px+. Global `button { min-height: 44px }` in CSS.
- [x] **2.7 Safe-area + viewport correctness.** `viewport-fit=cover`, `env(safe-area-inset-*)` everywhere.
- [ ] **2.8 Real-device testing.** iPhone SE 2020, iPhone 14+, Pixel 6a, iPad Mini.


## Phase 3 — Visual identity and design system

- [x] **3.1 Icons: Lucide React + Twemoji-as-SVG bridge.** `lucide-react` added. NAV, BOTTOM_TABS, MORE_ITEMS all use Lucide component refs (`E` property). Sidebar nav, bottom nav, MoreDrawer all render `<n.E size={...}>`.
- [x] **3.2 Iconography rule of thumb.** Custom/Lucide icon for navigation and primary identity. Twemoji for content (crops, animals). Native emoji ONLY for celebration moments. Three tiers, one rule each.
- [x] **3.3 Dark mode.** `[data-theme="dark"]` CSS token block + `@media (prefers-color-scheme: dark)` auto-detect. `darkMode` state with lazy initializer (localStorage + system pref). Moon/Sun toggle in sidebar footer + MoreDrawer. Persists to `localStorage` (`hfm_theme` key).
- [x] **3.4 Lucide icon swap pass.** Sidebar brand header: `🌾` → `<Leaf/>`. AI chat FAB header: `🌾` → `<Leaf/>`. Pantry + Financials delete buttons: `🗑` → `<Trash2/>`. `Trash2` added to imports. **Note:** remaining `🌾` instances are all content/data (AI suggestion chips, harvest label, `_catIcons.Grain`) — intentionally left as emoji per iconography tiers. **Also fixed in this session:** `Map` lucide import renamed to `MapIcon` to prevent Rolldown minification shadowing global `Map` constructor (was causing `TypeError: A is not a constructor` crash). `getRegionalCalendar` function restored — was dropped during Phase A data module extraction (commit 58cdf57), not exported from `regional-overrides.js`.
- [x] **3.5 Pill/badge consolidation.** `Pill` extended with `sm` (10px/2px 8px) and `border` props. 14 inline `<span>` pills across 7 locations replaced: task priority zone label, zone status, seasonal card (×2), seasonal detail (×3), farm crop picker (×3), manuals DIY card (×2), manuals DIY detail (×4). Notification badges (nav, attention button) left as-is — different semantic purpose.

## Phase 4 — Information architecture rethink

- [x] **4.1 Today replaces Dashboard.** `function Dashboard` → `TodayScreen`; NAV "Home"→"Today"; routing updated; "Back to Today" in FeedbackSurvey.
- [x] **4.2 Calm-default greeting.** Urgent highlight changed red→orange; copy: "a quiet day 🌿" / "N need attention" / "on your walk today". Background gradient also softened.
- [x] **4.3 Farm map promoted to hero on Farm tab.** `FarmMapHero` component — full-width zone map with crop patches, grid overlay, legend, empty-state CTA. *Superseded 2026-05-15 by Phase 6.9 — the original `FarmMapHero` was replaced with a PNG-based `LivingFarmMap` + click-zone overlay, while Phase 4.3's promotion-to-hero placement stays unchanged.*
- [x] **4.4 Module consolidation.** `FarmTab` wrapper with Map/Crops/Layout segmented control. `setup` removed from NAV + MORE_ITEMS; "Farming"→"Farm" in NAV. "Edit Map" in TodayScreen routes to `farm?tab=setup`. *Mobile/desktop "Livestock" vs "Animals" mismatch resolved 2026-05-11 by pulling 6.3.1 + 6.3.2 forward — both navs now show "Animals" with the `PawPrint` icon.*
- [x] **4.5 Manuals integrated into context.** "📖 Need help growing X? See the Manuals →" button in PlotOverlay (closes overlay, navigates to Manuals). Available wherever `setPage` is in scope.
- [x] **4.6 Settings out of the way.** Profile avatar section added at top of MoreDrawer (`User` icon in Lucide import; "My Farm / Free plan" label).


## Phase 5 — Today screen (the daily habit loop)

This screen is 80% of why someone keeps using MyTerra.

- [x] **5.1 Hero block.** Time-of-day greeting ("Good morning" / "Good afternoon" / "Good evening" / "Quiet hours" for pre-6am) replaces the "MyTerra" brand title on TodayScreen. Brand identity stays in the sidebar — the daily hero is meant to feel personal, not corporate. Weather snippet renders below the date when `data.city` is set: emoji + temp + condition + "in {location}" + gardening-actionable hint (e.g. "cool and cloudy in London — good for transplanting", "watch for frost — protect tender plants", "rain coming — skip watering"). Backed by **Open-Meteo** (free, no API key) via new `src/lib/weather.js`. Two endpoints: geocoding once per city (cached forever) + current weather (cached 1 hour). CSP updated in `vercel.json` to allow `api.open-meteo.com` + `geocoding-api.open-meteo.com`. Cache helpers in `src/lib/storage.js` (`loadWeatherCache` / `saveWeatherCache` / `loadGeoCache` / `saveGeoCache`). Fallback line when no city: "set your city for weather → Farm › Layout" pointing at where the city picker lives.
- [~] **5.2 Streak module.** Hero-sized streak band shipped below the Today header — 🔥 (≥7 days) or 🌱 (under 7) icon, big colored number with day count, supporting line showing personal best or celebration when at-or-past best. New copy for streak=0 ("Start your streak — Touch any task today to begin"). Last-7-days strip on the right of the band: filled green ✓ dots for active days (derived from `data.completions`), empty bordered dots for inactive, today gets a subtle ring. Old tiny streak corner card removed from the hero header — badges kept. Built `last7Days` useMemo dependent on `data.completions`. **Streak Freeze mechanic deferred to a follow-up commit** — it adds new schema (`freezesAvailable`, `lastFreezeGrant`), a migration, an `updateGamify` branch, and grant/consume timing rules that deserve their own design pass, not a rushed inclusion here.
- [x] **5.3 Three rings.** Apple Fitness pattern. Tasks done / Plants growing / Harvest readiness. Three nested rings shipped at TodayScreen.jsx lines 154-156. Rings themselves not tappable; cards directly below them are (drill-in works via card onClick). Third ring diverged from original plan ("Pantry stocked" → "Harvest readiness") — kept as shipped, harvest readiness is the more useful daily signal.
- [x] **5.4 "Start your walk" CTA.** Single primary button shipped between the streak band and the bento (full-width green pill, leaf + label + arrow, hidden when `enrichedTasks.length === 0`). Launches a new full-screen `WalkOverlay` (`src/features/today/WalkOverlay.jsx`, 259 lines) that snapshots today's tasks on open — attention items first (harvests, growing steps, periodic animal care due today, sorted by `pri`), then routine feed/water grouped by location. One task per screen with emoji, title, location, and a small type-label caption. Two paths to complete: **swipe up** (new `useSwipeUp` hook in `src/lib/use-swipe-up.js` — sibling to `useSwipe`, vertical-only, 80px threshold, abandons if horizontal motion wins) or **tap Done**; a Skip button advances without marking done. Completion calls `setData(markTaskDone(data, key))` — the same path TaskQueue's swipe-right uses, so streak/badges flow through the existing `updateGamify` wrapper. **Soft chime** via Web Audio API on each completion (660Hz→880Hz sine, 0.5s, no asset, no CSP change) + 15ms `navigator.vibrate` for haptic. End-of-walk summary card shows done/total count, contextual copy ("Walk complete" / "{N} of {total} done" / "Maybe later" / "Nothing for today"), and a "Streak started — day 1" badge when this was the first completion ever. Body scroll locked while overlay open, Esc closes. **Verified**: clean build (1770 modules), 7 marker strings present in live bundle `index-C-3tGAzz.js` (`Start your walk`, `swipe up to complete`, `Walk complete`, `Back to Today`, `Nothing for today`, `Maybe later`, `Streak started`). **Not verified — needs real-device testing**: swipe-up feel on iOS/Android, Web Audio chime on iOS Safari (should work since each completion is inside a user-gesture handler), `navigator.vibrate` on Android, overlay layout on small phones, button visual on actual screen.
- [x] **5.5 Recent activity feed — contextual CTAs.** New **Suggestions** section above Recent Activity on TodayScreen. Derives CTAs from current data state, not log history — when there's at least one fresh harvest item in Pantry (source=`harvest`, addedDate within last 7 days, unit=`kg`, qty>0), shows a row "{N}kg fresh {names} won't last forever" with a **"How to preserve →"** button that routes to Manuals. Section hides cleanly when no suggestions apply. **Deviation from plan**: the plan example "Yesterday you harvested 4kg mint — preserve some?" implies a log-history-driven CTA, but data-state is the stronger signal — the CTA is most useful while the produce is *still in Pantry*, regardless of whether the log entry has been pruned. This also avoids parsing user-facing log copy (fragile under copy changes). New `suggestions` useMemo keyed on `data.pantry.items`; section is the only place in the app that bridges Pantry items to the 13 preservation methods documented in Manuals — closes a real discoverability gap. Architecture is open for adding more suggestion patterns later (ready-to-harvest could become its own CTA if the Crops tile signal isn't enough).
- [x] **5.6 Glanceable secondary tiles.** Bento-grid below the streak band — mixed sizes, not equal-width KPI row. New `.bento` + `.bento-wide` responsive grid classes in `src/index.css` (desktop: 4-col with `.bento-wide` spanning 2; tablet ≤767px: 2-col with wide spanning both; mobile ≤479px: single column). `TodayScreen.jsx` info-boxes wrapper switched from `.g5` to `.bento`. Three tiles tagged `bento-wide`: Today's Work (the daily focal point), Growing (taxonomy summary), Money. Crops + Animals stay as 1-col tiles. Resulting layout: Row 1 = `[Today × 2] [Crops] [Animals]`, Row 2 = `[Growing × 2] [Money × 2]`.

## Phase 6 — Module-by-module fixes

### Tasks
- [x] **6.1.1** Replaced the right-side green "✓ Done" pill buttons with a left-side **Things-3-style circular checkbox** — new `TaskCheckbox` UI primitive in `src/components/ui.jsx` (22px circle, 2px border, fills green with white check on tap, `aria-label` "Mark as done" / "Mark as not done", `aria-pressed`, scale-up to 1.08 on the fill transition). `TaskRow` in `src/features/tasks/TaskQueue.jsx` now renders the checkbox before the emoji and the row's right edge is reserved for the Harvest CTA only — clicking the harvest button still routes to Farm (`onGoToFarm`) because harvest is a *goto* action, not a completion (the actual harvest commits inside `PlotOverlay`).
- [x] **6.1.2** **Animated-then-commit completion.** Tapping the checkbox (or swiping right — both paths converge on `handleComplete`) sets a per-row `completing` state that triggers the row's CSS transition: opacity 1→0, transform `translateY(8px) scale(0.97)`, strikethrough on title + emoji, all over 280ms `ease-out`. After the animation `setTimeout` fires the actual `onMarkDone` / `onToggleStep` callback so the parent removes the row from its section. Honors `prefers-reduced-motion` — completes instantly with no animation. Once `completing` flips, the row's `onClick` (detail-open) is disabled and the cursor reverts to default so a panicked second tap can't reopen the detail mid-fade. Not a true FLIP (no shared-element transition between sections) — judged that the fade+scale+strikethrough carries the perceived quality without the complexity of cross-section element re-parenting.
- [x] **6.1.3** **All group sections collapsible + friendlier calendar.** Added `attentionCollapsed` / `routineCollapsed` state (default expanded since they're the high-signal "today" sections); Farm This Week, Animals This Week, and Done Today were already collapsible. Calendar at the bottom got four friendliness changes: (a) **week now starts Monday** — `DAYS = ["Mon","Tue","Wed","Thu","Fri","Sat","Sun"]`, `firstDow = (calStart.getDay() + 6) % 7` — matches the Western Europe convention and the WE-baked content layer; (b) **today is a soft ring**, not a heavy green fill — 2px green border + bold green number on transparent background, while selected day keeps the full green fill + white number; (c) **Sat/Sun column headers muted** (opacity 0.6) so weekdays read as the primary plane; (d) **new "Today" jump button** beside the month label that only renders when you're viewing another month — one tap sets month + year + selects today's date. `aria-label`s added to the month-nav chevrons + Today button. Verified live at `https://myterra-sigma.vercel.app/app` on commit `30401f4`, bundle `index-DH3qk73J.js` — all 5 markers ("Mark as done", "Mark as not done", "Needs Attention", "Daily Routine", "Jump to today") present 1× each, Mon-first DAYS array confirmed, no leftover Sun-first array. **Not verified — needs human eye**: the checkbox + fade feel on a real device, weekend muting subtlety in actual UI.

### Farm (was Farm Layout + Farming)
- [x] **6.2.1** **Shipped under Phase 4.4 — already live.** `FarmTab` wrapper in `src/features/farm/Farm.jsx` exposes the Map / Crops / Layout segmented control; the legacy "Setup" route was folded in via the Layout tab when `setPage("farm", {tab:"setup"})` is invoked from elsewhere (TodayScreen "Edit Map" deep-links into it). Listed here for completeness — no separate ship needed.
- [x] **6.2.2** First-visit tutorial overlay on Farm Designer ("Drag a zone, name it, set its size").
- [x] **6.2.3** "+ Plant" CTA inside each zone card on the map — contextual.
- [x] **6.2.4** Crop detail opens as a bottom sheet, not full-screen.

### Livestock → Animals
- [x] **6.3.1** Renamed "Livestock" → "Animals" across all user-facing surfaces: desktop sidebar NAV (`src/app/navigation.js`), Animals page header (`Livestock.jsx`), Projects category color map (`Manuals.jsx`), Chicken Coop project category (`projects.js`), AI assistant output strings (`lib/ai.js`). FeedbackSurvey modules array also refreshed end-to-end (Dashboard→Today, Farm Layout+Farming→Farm, Seasonal Calendar→Seasonal, Livestock→Animals, "Smart offline farm assistant"→"Farm Assistant"). Component name `Livestock`, route id `"live"`, and code/data-file references kept unchanged.
- [x] **6.3.2** Sidebar icon swapped `Rabbit` → `PawPrint` to match the mobile bottom-tab icon. Plan note about "currently goat" was outdated — actual icon was `Rabbit`. `Rabbit` import dropped from navigation.js.
- [x] **6.3.3** **Deferred to post-Supabase.** Photos in the spec would need base64 in localStorage; with a 5MB quota and the kind of casual phone photos people take (1–5MB raw, ~200–400KB even after aggressive client-side resize), even one photo per animal blows the budget for power users. Plan: revisit after Phase 5 (Supabase Storage) so photos live as proper blob URLs, not in the key-value store. Feeding schedule + egg log will ship alongside photos to keep the animal-detail surface a single coherent pass rather than a half-done UI now and a re-do later.


### Seasonal
- [x] **6.4.1** **Month strip auto-scrolls to current month.** New `monthStripRef` + `monthBtnRefs` in `SeasonalCalendar` (`src/features/manuals/Manuals.jsx`). `useEffect` on `[month]` centers the active button horizontally inside the strip via `scrollBy({left, behavior:"smooth"})`. The strip itself changed from `flex-wrap` to `overflow-x:auto` so the 12 buttons live on one row that scrolls — current month is always in view on mobile without requiring a wrap layout. `aria-label` + `aria-pressed` on each month button.
- [x] **6.4.2** **Difficulty pill contrast.** `Pill` already accepts a `border` prop (added in Phase 3.5). The Seasonal CropRow's difficulty pill now passes `border={c.diff.c}` so each tier gets a colored outline as well as the tinted background — Easy=green, Medium=orange, Advanced=red — readable at a glance even when two pills sit next to each other in the same row. The emoji circle (🟢/🟡/🔴) was retained for redundancy (color + shape + label = three signals).
- [x] **6.4.3** **"New to farming?" tip on every month.** Old gate `isCurrentMonth && filter === "sow"` relaxed to just `filter === "sow"` so the tip appears whenever someone is browsing what to plant in any month, not only the current one. New `beginnerTip` `useMemo` pre-computes the two example crops + the empty-month case (`kind:"empty"` when no Easy crops are sowable in the selected month — copy switches to "flip through months to find a forgiving start"). Pre-computed to keep the JSX render path IIFE-free per the project's artifact-renderer constraints.

### Pantry
- [x] **6.5.1** **Hero count tile.** Replaced the two-up `Stat` row (Items + kg) with a single hero `Card` that combines them: 40px 🫙 emoji, big "{N} items, {kg} kg total" headline, and a dynamic tagline. `pantryHero` data pre-computed before `return` (no IIFE) — tagline picks between "Your harvest will live here" (empty), "{N} kg fresh from your farm — your shelf is starting to fill" (when `source:"harvest"` items exist), or "Stocked and ready" (manual items only). `Stat` import removed since it's no longer used here.
- [x] **6.5.2** **Empty-state mason jar + warmer copy.** Replaced the 📦 empty state with a 72px 🫙 illustration. Copy branches on filter context: when Pantry is fully empty → "Your shelf is bare" / "Your harvest will live here — plant something or add an item manually to get started." When a category filter has no items → "No {category} yet" / "Switch the filter above or harvest some {category} from your farm." Same `C.grdWarm` background gradient kept.
- [x] **6.5.3** **Eat button kept** — was already shipped, this checkbox was just documentation that it lives.

### Financials
- [x] **6.6.1** **Empty-state replaces the chart-only page when items.length === 0.** New illustrated card (🧾 72px) with "No money tracked yet" / "Add your first seed packet to start tracking. We'll handle the math — categories, monthly trends, profit and loss." plus a primary "+ Add your first entry" CTA. Stat row + chart + breakdown all hidden in this state — the empty-state card is the page. Add Entry overlay is still reachable from both the header button and the empty-state CTA. Body wrapped in a `<>` fragment after the empty branch to keep the JSX single-expression-per-return.
- [x] **6.6.2** **Chart gated on ≥2 data points.** New `monthsWithData`/`daysWithData`/`chartHasEnough` derivations. When the active view has fewer than 2 months/days with data, the chart svg is replaced by a friendly "Not enough data yet for a monthly/daily chart — add a second entry {in a different month / on a different day} to see the trend." The mode toggle + month-nav arrows stay visible so the user can still switch views, but the broken-looking single-bar chart no longer renders. Threshold of 2 is the minimum where a chart actually conveys trend information; a single bar is just a number.
- [x] **6.6.3** **Donut chart + legend.** Old horizontal-bar breakdown replaced with a real SVG donut. New `pieSlices` `useMemo` builds proper arc-path geometry: each slice's start/end angles in radians, SVG arc path `M 60 60 L x0 y0 A 50 50 0 large 1 x1 y1 Z`, large-arc flag toggles at >50%. Inner `circle r=28` creates the donut hole, with center text showing the total € amount. Legend on the right side: colored swatch + category + percentage + amount, sorted by spend descending. Category color palette covers Seeds/Tools/Feed/Animals/Fuel/Infrastructure/Produce Sales/Other. Single-slice edge case rendered as a solid circle (the arc math degenerates at full-circle).

### Manuals
- [x] **6.7.1** **Visual crop card grid.** Old `Card` rows that read like an admin table replaced with a responsive `grid-template-columns: repeat(auto-fill, minmax(180px, 1fr))` grid of square-ish cards. Each card: 38px emoji centered up top, name in 13px bold, category Pill in the crop's own color (`c.color + "22"` tint), and a one-line meta row "☀ {sun} · {days}d". Top border in the crop's color carries identity. Click anywhere on the card opens the same crop detail overlay as before. Empty search state added too: "🔍 No crops match '{query}'".
- [x] **6.7.2** **Sticky search bar.** Crops-tab search input wrapped in a `position:sticky; top:0; z-index:5; background:C.bg` container so it stays anchored to the top of the scroll area as the user browses the card grid. Padding gives it room to breathe over the cards as they pass underneath.

### Farm Assistant FAB
- [x] **6.8.1** **Lucide icon swap.** Replaced the 🌾 emoji with a `MessageCircle` (26px) overlaid by a small `Leaf` (12px) — a chat-bubble + sprout composition that signals "ask the farm assistant" without leaning on a culture-specific emoji. The closed state shows the × glyph. Background gradient (`C.grd`) kept; the icon is white-on-green at all times.
- [x] **6.8.2** **30s attention pulse when there are urgent plots.** New `urgentCount` `useMemo` counts plots whose elapsed-since-plant is past `crop.days` (same intent as `pri:0` harvest tasks in `buildTaskQueue` — over-ripe produce is the canonical "needs attention" signal). When count > 0 and the panel is closed, the FAB pulses for 1.4s on mount and again every 30s via a new `@keyframes fabPulse` (expanding ring via `box-shadow`, no extra DOM). Honors `prefers-reduced-motion`. An orange numeric badge (top-right of the FAB) shows the count while the panel is closed; "9+" caps the display.
- [x] **6.8.3** **Position respects the bottom-tab bar on mobile.** New tiny `useState`/`resize` viewport tracker inside the component (no shared hook to avoid touching the wider codebase). When viewport < 768px, the FAB's `bottom` jumps from `24px` to `calc(72px + env(safe-area-inset-bottom))` — clears the 56px BottomNav plus 16px gap plus iOS safe-area inset. The chat panel anchor also follows (`calc(140px + env(safe-area-inset-bottom))`) so the open panel doesn't get clipped by the tab bar. `transition: bottom .2s` so the FAB doesn't jump abruptly when rotating a device across the breakpoint.

### Living Farm Map (PNG-based, post-Phase 8.5)
- [x] **6.9.1 PNG-based Map view replaces `FarmMapHero`.** New folder `src/features/farm/living/` with five files (~530 lines): `paths.js` (URL helpers — exports `BACKGROUND_PNG`, `zonePngPath(type)`), `ZoneImage.jsx` (shared `<img>` with placeholder fallback — on 404 falls back to a tinted dashed-border div with the zone-type emoji centered), `LivingFarmMap.jsx` (read-only Map view — background `<img>` + one `<ZoneImage>` per `data.zones` entry positioned by `xM/yM/wM/hM` as percentages of `farmW/farmH`; click a zone opens `ZoneOverlay`), `ZoneOverlay.jsx` (click-zone details using the shared `Overlay` chrome — size strip, fill %, crop list / animal list, Plant / Edit Layout actions), `ZonePalette.jsx` (10 draggable zone-type tiles with placement-count badges). The original `FarmMapHero` (~167 lines in `Farm.jsx`) deleted in the same commit. **Preserves Phase 8.5 behaviors**: time-of-day tint overlay, urgent-zone pulse class, edit-layout button. *Watered-plot glisten on individual crop patches dropped intentionally — the new map shows zones as a single PNG tile, not as crop-patch-per-plot mini-visualizations; per-plot growth bars + 💧 indicators no longer have a place to live on the map. Plot-level signals stay accessible via TaskQueue + PlotOverlay.*
- [x] **6.9.2 Drag-from-palette zone creation in Layout (Setup) designer.** Canvas wrapped with palette in a flex container (`.lf-wrap`); palette renders as right sidebar on desktop, **horizontal strip above the canvas on mobile** (`@media (max-width: 767px)` flips wrapper to `column-reverse`). HTML5 drag-and-drop: drag a tile from the palette → drop on canvas creates a new zone at the drop position (default 10m × 8m, snapped to 1m grid, with sensible default name like "Vegetable Bed" or "Vegetable Bed 2" if duplicates exist). Existing zone-rename / drag-to-reposition / resize-from-edges all preserved. The "+ Zone" button stays as a fallback for the no-drag path. Custom drag mime type `application/x-myterra-zone-type` carries the zone type id.
- [x] **6.9.3 Zone fills replaced with PNG tiles (transparent rectangle + `<ZoneImage>`).** In both the Map view and the Layout designer canvas, each zone rectangle now renders the zone's image (`/zones/{type}.webp`) instead of a solid `zt.fill` color. The frosted name pill sits on top. `<BackgroundLayer>` renders the background image (`/zones/background.webp` — WebP for bandwidth, see 6.9.6) behind the grid + zones in the Layout designer.
- [x] **6.9.4 8% safe-area inset on drop position + existing-zone drag clamps.** Both new-zone drop and existing-zone drag clamps now require zones to sit inside `[0.08 × farmW, 0.92 × farmW − wM]` on X and the equivalent on Y, so zones don't collide with the painted fence/trees around the background's edges. Existing user zones that sit outside the safe area are not migrated — they snap to the safe area on first user drag, no data migration needed.
- [x] **6.9.5 Fallback when PNGs missing.** Every layer has an `onError` fallback: background → sage tint (`--farm-bg-fallback: #d9e7d6`); each zone tile → tinted dashed-border placeholder with the zone-type emoji + name. App is fully usable with zero PNGs in `public/zones/`. The day real PNGs land in `public/zones/`, every tile updates on next page load with zero code changes.
- [~] **6.9.6 Background switched to WebP (1200×900 q80, 151 KB).** First shipped as a 596 KB JPEG (commit `df83f56`), then re-encoded to WebP after a bandwidth-benchmark across 10 strategies. Final path: `/zones/background.webp` at 1200×900 (downsized from 1448×1086 — the canvas renders inside an ~800px-wide container on most screens, so 3× more pixels was wasted bandwidth), WebP quality 80. Result: **151 KB (25% of the original JPEG, 6% of the lossless PNG).** `paths.js` switched from `.jpg` → `.webp`; zone tile resolver `zonePngPath(type)` also switched to `.webp`. New helper script `scripts/optimize-zone-image.mjs` (Node + `sharp` devDep, no Homebrew needed) converts source PNGs to the right WebP recipe — `background` (1200×900 q80) or `zone` (800×600 q80, target 50-90 KB each). **As of 2026-05-21 only 4 of the 10 zone tiles ship**: `house.webp`, `herbs.webp`, `orchard.webp`, `veg.webp`. The remaining 6 zone types (chicken/pasture/hive/pond/storage/path or whichever set the palette exposes) render with the placeholder fallback from 6.9.5 — usable, but visually inconsistent against the painted background. Marked `[~]` until the remaining 6 tiles land. **Total bundle budget realised so far**: background 151 KB + 4 zone tiles × avg 118 KB ≈ **~625 KB**; projection holds for the full 10.

**Markers verified in live production bundle `index-DLi3gezz.js`**: `Click a zone to open details`, `Drag to place`, `application/x-myterra-zone-type`, `data-living-zone-marker`, `data-zone-palette-marker`. Plus CSS classes `.lf-wrap` / `.lf-canvas` / `.lf-palette` and CSS var `--farm-bg-fallback` in `index-D0zEFmmU.css`. Commit `418b366` → deployment `dpl_9uycVXi8VFfSCCdYtR41sigjyca1` → live URL https://myterra-sigma.vercel.app/app. **Not verified — needs human eye**: how the placeholder zone tiles read against the scenic background once it lands, whether the 8% safe-area feels right on small phones, palette horizontal-scroll feel on mobile.

### 6.10 Living Farm Map polish + Walk Overlay (2026-05-15 → 2026-05-21)
Six days of shipped polish that landed between the 2026-05-15 Phase 6 freeze and the 2026-05-20 accidental file deletion. None of this scope was planned in advance — it was a tight iteration loop driven by real-device feedback on the Living Farm Map and Walk Overlay.

- [x] **6.10.1 Living Farm Map redesign — "minimal living planner".** Commit `66bec76`. Stepped away from the painted-PNG scenic background as the primary surface and re-positioned the map as a calmer, planner-style canvas with stage-aware crop patches sitting on simpler zone tiles. New helpers in `src/features/farm/living/visuals.js`. Removed `ZoneImage.jsx` + `paths.js` from the living folder.
- [x] **6.10.2 Stage-aware crop patch rendering.** Commits `dd54b24` + `9834419`. Each crop patch now reflects its growth stage visually (seedling/growing/ready/over-ripe) via the new `CropStagePatch` component (~176 lines). Progress gauges added: a thin circular indicator showing days-elapsed / days-to-harvest for each patch. Replaces the old generic colored rectangles.
- [x] **6.10.3 Auto-stack crop patches.** Commit `2fec54b`. Drag/resize on crop patches removed — patches now auto-stack in rows inside their zone. Trade-off: less individual control, but eliminates the "patches floating randomly" mess users were generating. Commit `420eba1` (the most recent) unified the row-pack algorithm across all map surfaces (Dashboard mini-map, Farm map, Walk map) so layout is consistent everywhere.
- [x] **6.10.4 Editor pointer events.** Commit `a175943`. Farm Layout designer drag/resize handlers converted from mouse events to pointer events — fixes touch responsiveness on iPad and Android tablets where mouse events fire inconsistently.
- [x] **6.10.5 Map unification — Dashboard + Walk share `LivingFarmMap`.** Commit `a5fdb12`. Eliminates duplicated render logic between `TodayScreen`'s mini-map and the Farm tab. The same `LivingFarmMap` now serves both, with `fitMode` ("aspect" | "fill"), `interactive`, `onZoneClick`, and `showCropPatches` props parameterizing the differences. ~232-line consolidation in `LivingFarmMap.jsx`.
- [x] **6.10.6 Walk Overlay fit + portal correctness.** Eight rapid fixes between 2026-05-19 and 2026-05-21 (commits `f472cb3`, `09b4137`, `f3be537`, `7c19602`, `c5ba9be`, `3c487bf`, `fd4baff`, `420eba1`). The walk-mode map went through `aspectRatio` → `fill mode` → `height:0 flex fix` iterations before settling on **contain-fit / no scroll / no pan-zoom on any screen size**. Also: WalkOverlay now portals to `document.body` (via `createPortal`) so the bottom-tab nav can't clip the bottom of the overlay. Task card anchored below the map with no float/overflow. **Real-device tested 2026-05-21 by Dervis — final layout works on all tested screens.**
- [x] **6.10.7 framer-motion removed.** Commit `1db4515` (2026-05-19). 8.2 spring physics + 8.3 shared-element transitions both reverted to native CSS. See 8.2/8.3 entries above for rationale + verification.

### 6.11 Map v3 + FarmIcon system (2026-06-10)
Design pass requested by Dervis: map as clean/modern/informative as possible (stages + fill % on the map itself), and no crop/animal left rendering as a colored-dot emoji ("red dot for radishes").

- [x] **6.11.1 Custom SVG icon set — `src/components/FarmIcon.jsx` (new, ~330 lines).** 25 flat 2-tone icons for every crop/animal with no real Unicode emoji: Radish, Beetroot, Turnip, Leek, Celery, Celeriac, Pea, Okra, Plum, Apricot, Persimmon, Fig, Pomegranate, Lentil, Chickpea, Rhubarb, Lavender, Raspberry, Blackberry, Walnut, Almond, Artichoke, Asparagus, Quail, Guinea Fowl. `<FarmIcon name emoji size/>` falls back to the data emoji for everything else. Wired into 12 surfaces: map crop patches, Manuals crop grid + both detail titles + animal list + Seasonal/Maintenance rows, Farm crop picker, Onboarding plant cards, PlotOverlay/AnimalOverlay titles, Livestock rows, WalkOverlay task cards (via new `cropName` field on crop tasks in `task-queue.js`), Today zone-inspector gauges, ZoneOverlay crop/animal rows. **Follow-up shipped same day:** task rows render FarmIcon too — TaskQueue rows, day-detail rows, calendar-cell minis, Done Today, and the Today pipeline (events/done-builders now carry `cropName`/`speciesType`); text emoji remains only inside activity-log strings.
- [x] **6.11.2 CropStagePatch v3.** Old patch stacked 4 labels (% pill, name, variety, stage pill) over fixed-size texture blobs + a rising fill — unreadable at mini-map sizes. Rebuilt icon-first: crop icon in a white chip with an SVG progress ring (pathLength=100 trick, growth %), stage tints sand→green→amber from new `STAGE_STYLE` tokens in `visuals.js`, name + % only when room (`showText && !compact`), 🧺 badge + amber glow when ready. Mini-map renders chip-only.
- [x] **6.11.3 Zone chrome v2.** Dark translucent name blob → frosted light pill: zone-type icon + name + **fill %** color-coded via new `zoneFillColor` (green/amber/red at 70/95). Barn/pasture zones get animal token chips (species icon × head count, +N overflow) from new `zoneAnimalGroups(zone, animals)` helper — animals aren't zone-linked in data v1, so it mirrors the dashboard housing rule (poultry+rabbits→barn, grazers+pigs→pasture). Layout designer's own name blob restyled to match.
- [x] **6.11.4 Canvas polish.** Background flatter/lighter, vignette ~halved, roads 14→9 px and lighter, harvest-ready pulse red→amber `#e08a2e` (ready-to-harvest is good news, not an error) incl. reduced-motion fallback.
- [x] **6.11.5 Dashboard + overlay fixes.** (a) Stray literal `)` that rendered as text beside the mini-map legend — removed. (b) Crop-color legend lied (CROP_COLORS swatches never matched patch visuals) — replaced with honest 3-stage legend on Today *and* in the Layout designer (whose legend was also an IIFE in the render path, violating renderer rule #4). (c) ZoneOverlay animal list was dead code — filtered on nonexistent `a.zone`, read nonexistent `k.icon`/`k.label` (fields are `e`); now uses `zoneAnimalGroups` + FarmIcon and actually populates.
- [ ] **6.11.6 Real-device pass by Dervis.** Icon legibility at mini-map chip sizes, pill contrast on each zone surface, ring readability, dark-mode check, SVG icon quality judgment (hand-drawn in code — replaceable per-icon inside `FARM_SVG` without touching call sites).

## Phase 7 — Onboarding (the first 90 seconds)

- [x] **7.1 Story-style 4-screen intro.** Welcome → Where do you grow (region + city) → Your growing space (zone type + size, no drag) → Pick your first plants (up to 3 Easy-tier crops filtered by current month + region). New file `src/features/onboarding/Onboarding.jsx`. Triggered from `App.jsx` when `!data.setupDone && data.zones.length === 0` — fires on first visit AND after data reset. **Deviation from plan:** "drag one zone" simplified to card-picker (zone type + size selector); creates a real zone + plots in data on finish. `ScreenPlants` prefers crops sowable within ±2 months of current date, falls back to top 6 Easy crops off-season.
- [x] **7.2 End on a win.** On finish, `handleFinish` writes the zone + plots into data with `setupDone:true`. `buildTaskQueue` immediately emits water/care tasks for the planted crops — Today screen shows them on first arrival. No artificial "Water your basil" hardcode needed; the real task engine generates it.
- [x] **7.3 Skip-friendly.** "Skip — I'll set up manually" on screen 1 writes WE defaults with `setupDone:false` (clears the overlay, no zone created, user lands on the blank app and can use Farm Setup manually). Each subsequent screen has a Back button. No forced forward progression.
- [x] **7.4 No login wall.** Pure localStorage path — no auth, no network, no gating. `onComplete` calls `setData` directly. Verified: bundle `index-BLqbc0kD.js`, all 6 markers present (Get started, Where do you grow, Your growing space, Pick your first plants, Start farming, Vegetable Bed).


## Phase 8 — Motion polish

- [x] **8.1 FLIP for every list reorder.** Hand-rolled `useFlip` hook (`src/lib/use-flip.js`) — no new dependency. snapshot/flip API: F captures `getBoundingClientRect()` before state change, L reads new position after React re-render, I applies inverted `translate(dx,dy)` instantly, P animates to zero (320ms ease-out-quad). Done section auto-expands on any completion so the FLIP destination is always visible. `rowRefs` map stores live DOM nodes for completable rows; `pendingFlips` Set holds keys awaiting their Done row mount; Done rows call `flip(key, el)` on mount. `markDone` snapshots source row → opens Done → commits state. Scoped to TaskQueue Attention + Routine rows. Honors `prefers-reduced-motion` (no-ops cleanly). Verified: bundle `index-C61z2aKG.js`, full FLIP logic present including `getBoundingClientRect`, `translate`, `pendingFlips`, `onRef`. **Not verified — needs real-device feel**: the glide timing on iOS/Android, whether 320ms is right for the perceived distance.
- [x] **8.2 Spring physics on draggable elements.** *(Originally shipped with Framer Motion; reverted to native CSS in commit `1db4515` on 2026-05-19 for bundle-size + iOS touch performance.)* Now backed by hand-rolled CSS transitions + pointer events: bottom-sheet drag-to-dismiss uses inline transforms during drag and snaps back via `transition: transform .2s ease-out` on release; swipe-to-complete uses the existing `useSwipe` hook (touch-only, `pan-y` for vertical pass-through). The `src/lib/use-spring-drag.js` module and the four spring presets were removed alongside `framer-motion`. **Real-device tested 2026-05-21 by Dervis — feel is acceptable; no regressions worth chasing the framer-motion dep back for.**
- [~] **8.3 Shared-element transitions.** *(Reverted in commit `1db4515` on 2026-05-19 when `framer-motion` was removed.)* The `layoutId` crop-card → PlotOverlay shared-element animation depended on Framer's layout engine; there is no native CSS equivalent for cross-component shared-element transitions today (View Transitions API still has uneven browser support and breaks inside Capacitor WebViews). Card → overlay now uses the standard sheet slide-up via the `Overlay` component. **Real-device tested 2026-05-21 by Dervis — acceptable.** Marked `[~]` rather than `[x]` because the specific "card geometry morphs into the sheet" behavior is no longer in the app; revisit only if the View Transitions API matures or if a `layoutId`-equivalent ships in a lighter library than framer-motion.
- [x] **8.4 Achievement = full-screen moment.** Wired a new `<BadgeCelebration>` overlay (`src/components/BadgeCelebration.jsx`) into the app shell. When `updateGamify` (`src/lib/migrations.js`) earns a badge that has never been celebrated, it attaches a transient `_pendingCelebrations: [...badgeIds]` field to the returned data. `setData` in `App.jsx` strips that field before persisting and pushes the ids onto a new top-level `badgeQueue` state, which the celebration component consumes one at a time. The overlay renders the badge's emoji (72px, drop-shadow), name, and description on a centered card that scales in with overshoot bounce via the existing `badgeUnlock` keyframe (`cubic-bezier(0.34, 1.56, 0.64, 1)`), surrounded by 12 directional confetti pieces flying outward — the `confettiBurst` keyframe was rewritten to use CSS custom properties (`--cx`/`--cy`) so each piece travels in its own pre-computed direction (radial layout, 6 colors, staggered 30ms emission, 1.2s duration). 1.8s auto-dismiss; tap-anywhere also dismisses. `navigator.vibrate(150)` fires on mount (Android-only; silently no-ops on iOS). `prefers-reduced-motion` honored: confetti suppressed entirely, badge card fades in instead of bouncing, vibration set to 0. On dismiss, the badge id is appended to a new persisted `gamify.celebratedBadges` array so it never shows again — `migrateGamify` backfills this array with every existing badge id for users who earned badges before this feature shipped (no retroactive surprise overlays). Verification: build clean, ESLint clean, bundle `index-CVKYlB7B.js`. **Not verified — needs human eye**: the feel of the overshoot + confetti on a real device, the Android vibration kick, and whether 1.8s is the right hold (might want longer for the "Companion Planner" / "Zone Master" types with denser desc text).
- [x] **8.5 Living farm map.** Watered plots subtly glisten for an hour. Urgent zones pulse red. Day/night affects map tint slightly. Plants "grow" — % indicator increases over time. Implementation: `FarmMapHero` in `Farm.jsx` — (1) `useState`/`setInterval` (60s tick) for current hour → day/night tint overlay on the map container (transparent daytime 8-17h, amber at dawn/dusk 6-7h/18-19h, dark blue at night 0-5h/20-23h); (2) `wateredPlotIds` derived from `data.completions[todayKey]` — patches whose plot id matches get the `map-glisten` CSS class (diagonal shimmer keyframe) + a 💧 indicator; (3) `isUrgentZone` per zone — any plot past `harvestDate` triggers `urgent-zone-pulse` CSS class (expanding red ring via `box-shadow` keyframe); (4) `growthPct` per patch — `daysSincePlant / totalDays` → thin 3px bar at the bottom of each crop patch, green → amber → red as it approaches/passes harvest. Two new keyframes in `index.css`: `mapGlisten` + `urgentZonePulse`. `prefers-reduced-motion`: animations replaced with static tints/outlines. Verified: bundle `index-BaHi0xe1.js` — `map-glisten` ×1, `urgent-zone-pulse` ×1, `growthPct` ×4, `isWatered` ×3, all present. **Not verified — needs human eye**: glisten subtlety in daylight, night tint strength on OLED, growth bar visibility at small zone sizes.
- [x] **8.6 Reduce-motion compliance.** Already in CSS — extend to JS animations. Test with system setting on.
- [x] **8.7 Skeletons not spinners.** Use existing `shimmer` keyframe consistently for any data load.

## Phase 9 — Microcopy and tone pass

- [ ] **9.1 String audit.** Replace "TODAY'S WORK" → "Today's farm walk." "8 urgent" → "Three things to do today." "Manage your animals, collect produce, track care" → "Your crew."
- [ ] **9.2 Empty-state copy.** Each empty state: one line of warmth + one CTA.
  - Pantry empty: "Your shelf is bare — let's plant something."
  - Animals empty: "No crew yet — chickens are a forgiving first try."
  - Financials empty: "No expenses yet — add your seed receipts and we'll handle the math."
- [ ] **9.3 Error messages.** Every alert/console error needs a friendly equivalent. "Couldn't save — your phone may be offline. We'll sync when you're back."
- [ ] **9.4 Microinteractions speak.** Button labels morph after tap: "Mark watered" → "Watered ✓". Tiny toast confirmations.

## Phase 10 — Accessibility

- [ ] **10.1 WCAG 2.2 AA contrast** on every text/background pair. Tokens make this a one-pass fix.
- [ ] **10.2 Screen reader labels.** Every emoji-only or icon-only button gets `aria-label`.
- [ ] **10.3 Color is never the only signal.** Urgency pairs color with shape (icon, dot, indicator).
- [ ] **10.4 Keyboard nav on desktop.** Tab order, focus rings, escape closes sheets.
- [ ] **10.5 Dynamic Type.** Use `rem` not `px` so user zoom works.


## Phase 11 — Grove restructure (premium reset, decided 2026-07-02)

**Decision (Dervis, 2026-07-02, after two mockup rounds — paint round + structure round):** MyTerra's home becomes the farm itself. **Grove: the map is the app.** Full-screen isometric farm scene as home; tasks rendered as markers pinned to their real place (basket over ready crops, droplet over thirsty beds, egg count over the coop); bottom task sheet; 4-item dock (Map · Animals · Pantry · More). **Paint: Daylight system** — light iOS-native chrome (all-Inter, single green accent, hairlines, de-tinted surfaces), chosen for outdoor mid-walk readability; the scene follows real time of day (extends the shipped 8.5 day/night tint); dark mode stays as the user toggle with a forest-charcoal token set. The scene is the signature element; chrome stays quiet. Supersedes the Notion × Planta lane. Structure and paint were decided independently (concept mock: `myterra-ui-concepts.html`; paint mock: `myterra-directions.html`, both delivered in chat 2026-07-02).

Build order is risk-first: the scene engine ships *inside the current shell* (replacing the LivingFarmMap renderer) and gets validated on real data before any navigation changes.

- [x] **11.1 Token re-valuing.** `:root` + `[data-theme="dark"]` + the `prefers-color-scheme` duplicate re-valued in place, zero token renames: Daylight light palette (bg `#f4f6f4`, accent `#128147` — white-text safe), forest-charcoal dark toggle (bg `#0f1512`, accent `#43d17f`, feedback-color dark overrides added, `--farm-bg-fallback` dark override added), rainbow tints collapsed to neutral elevation + whisper hues, shadows softened/diffused, Plus Jakarta Sans removed (Inter 400/600/700/800; `F.head` → Inter — display voice is weight + tracking), `C.grd`/`C.grdHero` now var-based so they re-theme, theme-color metas split light/dark, manifest colors updated, stale "Mediterranean focus" meta/manifest descriptions corrected.
- [x] **11.2 GroveScene engine.** Isometric projection of `data.zones` (axis-aligned rects → iso plates with side faces), grass ground + iso grid + footpath, zone tags. Replaces the LivingFarmMap render path behind the existing Farm tab + dashboard mini-map first (same `fitMode`/`interactive`/`onZoneClick` prop surface).
- [x] **11.3 Crop sprites + stages on the scene.** Billboard FarmIcons standing on plates with soft ground shadows; stage = sprite size/species (sprout → mid → crop icon); ready = amber halo pulse. Row-pack placement reuses the existing unified algorithm.
- [x] **11.4 Buildings + trees.** Coop/barn hut for animal-housing zones (hen sprites + head count), orchard trees for tree crops, hive/pond/storage treatments for the remaining zone types.
- [x] **11.5 Task markers.** Derived from `buildTaskQueue`: floating chips (basket/droplet/egg/care) with bob animation, count badges, tap → task action or PlotOverlay. `prefers-reduced-motion` honored.
- [x] **11.6 IA flip.** Scene becomes home behind `/app`; bottom task sheet (snap points: peek/half/full) absorbs Today's task pipeline; glass status pills (weather · streak); dock Map · Animals · Pantry · More; Start-your-walk lives in the sheet; Today's remaining content (suggestions, activity) redistributes into sheet + More.
- [x] **11.7 Screen-by-screen migration.** Animals / Pantry / Financials / Manuals / Onboarding onto the Daylight language — kill leftover tint semantics + raw-px overrides, one commit per screen.
- [x] **11.8 Motion pass.** Marker bob, stage-change grow transition, day/night scene shift, sheet physics. CSS-first, reduced-motion compliant.
- [x] **11.9 Living scene + zone detail card.** Crop sprites and orchard trees idle-sway (staggered, GPU transform-only); sprites pop-in on mount. Tapping any zone on the Grove scene springs up GroveZoneCard: per-crop rows with animated % completion bars, stage chips (Planted/Growing·~Nd/Ready), animal counts, Plant-here + Edit-layout actions. Flat Layout editor keeps its original ZoneOverlay. All animations transform/opacity for mobile 60fps; reduced-motion honored.

---

## Suggested execution order (5–7 weeks if Claude patches the codebase)

| Week | Focus | Status |
|---|---|---|
| 1 | Phase 1 (tokenize, type, components) + Phase 3.1–3.2 (Lucide+Twemoji swap) | ✅ Done |
| 2 | Phase 2 (mobile breakpoints + bottom-tab nav + stack layouts) | ✅ Done (2.5 + 2.8 deferred) |
| 3 | Phase 3.3–3.5 (dark mode, icon swap pass, pill consolidation) | ✅ Done |
| 4 | Phase 4 + Phase 5 (IA rethink + Today screen redesign) | ✅ Phase 4 done; Phase 5 complete except 5.2 Streak Freeze (deferred) |
| 5 | Phase 6.1–6.9 (module-by-module fixes) | ✅ Done — all 11 open items shipped (6.3.3 + 6.9.6 remaining-tiles deferred) |
| 6 | Phase 7 onboarding + Phase 8 motion polish | ✅ Done — 7.1–7.4 + 8.1, 8.4–8.7; 8.2 reverted to CSS, 8.3 dropped |
| 7 | Phase 6.10 Living Farm Map polish + Walk Overlay | ✅ Done — 6.10.1–6.10.7 (unplanned, driven by real-device feedback) |
| 8 | Phase 9 + Phase 10 (microcopy + a11y) | not started |

**Next up: out of UX/UI scope** — the architecture-side Phase 5 (Supabase auth + cloud backup) is the launch-blocker now. Phase 9 (microcopy) and Phase 10 (a11y) remain open in this plan but are lower-priority than getting accounts + cloud + monetization in place. Resume Phase 9/10 after Phase 5 ships, or pull them forward only if a specific microcopy/a11y issue surfaces during beta.

## Where this plan ends

When all checkboxes above are ticked, MyTerra is ready for the *next* big plan — the native-shipping plan (Capacitor wrapper, App Store, monetization). That plan is parked. Don't touch it until this one is mostly green.

## Cleanup backlog

Surfaced during the 2026-05-11 sync pass and knocked out before Phase 5 work resumed.

- [x] **Delete `src/App.jsx.backup`.** 239 KB stale snapshot, git-tracked. Residue from the Phase 3.11 sandbox mishap noted in ARCHITECTURE_PLAN's decision log (2026-05-11 entry). Removed via `git rm`.
- [x] **Add `output/` and `.playwright-cli/` to `.gitignore`.** Both were untracked Playwright debug artifact dirs (2.2 MB + 216 KB) at repo root. Added under a new "Playwright local artifacts" comment block.
- [x] **Retire `PROGRESS.md`.** Last updated 2026-03-31, still said "Mediterranean/Albanian agriculture focus is non-negotiable" — directly contradicted the 2026-05-05 Western Europe pivot recorded in ARCHITECTURE_PLAN. Removed via `git rm`. Launch/business tracking is parked until we're closer to a real ship date.

## Decision log

| Date | Decision |
|---|---|
| 2026-05-07 | Lane: Notion × Planta hybrid |
| 2026-05-07 | Palette: keep cream + forest, refine tones |
| 2026-05-07 | Icons: Lucide + Twemoji bridge, custom set later |
| 2026-05-07 | This file is the working plan; updates merged via PR |
| 2026-05-07 | Phase 1 complete — tokens in `:root`, C refs CSS vars, type scale exported as `TS`, 3 stray buttons fixed |
| 2026-05-07 | `C.r` / `C.rs` stay numeric — `C.r+4` arithmetic in Overlay means CSS vars would break it |
| 2026-05-07 | ~15 inline pill `<span>` elements deferred to Phase 3.5 (not worth touching before Pill gets tone variants) |
| 2026-05-07 | Phase 2 complete (2.1–2.4, 2.6, 2.7). Deferred: 2.5 swipe gestures, 2.8 real-device testing |
| 2026-05-07 | Phase 3.1–3.2 complete — lucide-react installed, all nav/tabs/drawer use Lucide component refs |
| 2026-05-07 | Phase 3.3 complete — dark mode CSS tokens + JS toggle + localStorage persistence + system-pref detection |
| 2026-05-07 | Phase 3.4 complete — Leaf icon in sidebar brand + AI FAB, Trash2 for delete buttons. Remaining 🌾 are content emoji (intentional). |
| 2026-05-07 | Bug: lucide `Map` import shadows global `Map` constructor after Rolldown minification → fixed as `Map as MapIcon`. Rule: any lucide import matching a JS global MUST be aliased. |
| 2026-05-07 | Bug: `getRegionalCalendar` dropped during Phase A Commit 4 data extraction, never exported from regional-overrides.js → restored to App.jsx before DEF block. |
| 2026-05-07 | Phase 3.5 complete — `Pill` extended with `sm` + `border` props. 14 inline spans replaced across 7 locations. Nav/attention count badges left as-is (different semantic). |
| 2026-05-07 | Phase 4 complete — TodayScreen, FarmTab (Map/Crops/Layout), FarmMapHero, calm greeting, manual link in PlotOverlay, profile avatar in MoreDrawer. |
| 2026-05-07 | `setup` route removed from top-level routing — now lives as "Layout" subtab inside FarmTab. `setPage("farm", {tab:"setup"})` bridges old callers. |
| 2026-05-07 | `setPage` made optional in PlotOverlay — callers without setPage (Farming subtab) silently skip the manual link CTA. No cascading prop drilling needed. |
| 2026-05-07 | `FarmMapHero` zone-render logic is a deliberate duplication from TodayScreen mini-map — acceptable until shared component refactor in Phase 6+. |
| 2026-05-11 | Sync pass against shipped code. **5.3 flipped to done** — three nested rings already shipped at TodayScreen.jsx 154-156; third ring is "Harvest readiness" rather than "Pantry stocked" — kept as shipped, harvest readiness is the more useful daily signal. **5.2 + 5.5 reclassified as partial** — small streak stat and plain log feed exist on Today, but neither matches the full plan (no hero streak, no Streak Freeze, no contextual activity CTAs). **4.4 annotated** with the BOTTOM_TABS "Animals" / NAV "Livestock" name inconsistency — same screen, two labels — to be reconciled in 6.3.1. Cleanup backlog added (App.jsx.backup, gitignore, PROGRESS.md). |
| 2026-05-11 | **6.3.1 + 6.3.2 pulled forward** to resolve the 4.4 mobile/desktop label mismatch in one shot. Renamed "Livestock" → "Animals" across all UI surfaces (sidebar NAV, page header, Projects category, AI assistant strings) and refreshed FeedbackSurvey's obsolete module list end-to-end. Sidebar icon swapped `Rabbit` → `PawPrint` to match the mobile bottom tab; `Rabbit` import dropped. Internal names (`Livestock` component, `"live"` route id, data files, "Livestock:" sub-tip inside Perimeter Fencing method content) intentionally left as-is — scope limited to user-facing UI labels. Bundle clean: zero "Livestock" matches for UI/nav strings; 18 "Animals" matches present. |
| 2026-05-11 | **2.5 swipe gestures partial** — swipe-right-mark-done shipped on TaskQueue + Today task pipeline; swipe-left-remove shipped on Pantry. New touch-only `useSwipe` hook in `src/lib/use-swipe.js`; new `SwipeableRow` wrapper in `src/components/ui.jsx`. Hook tracks `dragging` state so transition timing is correct (instant during finger drag, 150ms snap-back on release, 220ms commit slide-off). Resistance past threshold (35% scale-down). Touch handlers only — `touchAction: pan-y` reserves horizontal for us while letting vertical scroll pass through natively. Mouse interactions on the wrapped children are untouched. Animals skipped (no clean delete action). **Pull-to-refresh deferred** to post-Supabase (Phase 5) — shipping a dead lever would be worse than not shipping. Bundle +2.4 KB. |
| 2026-05-11 | **5.1 weather hero shipped** — new `src/lib/weather.js` fronts Open-Meteo (free, no API key). Two endpoints, both cached: geocoding once per city forever, current weather 1-hour TTL. WMO weather codes mapped to emoji + description + gardening-actionable hint via `buildHint` (frost → "watch for frost", thunder → "stay indoors", rain coming → "skip watering", cloudy + cool → "good for transplanting", clear + warm → "water early", etc). CSP updated in `vercel.json` to allow `api.open-meteo.com` + `geocoding-api.open-meteo.com`. TodayScreen hero: "MyTerra" brand title replaced with time-of-day greeting; weather snippet renders as a third line under the date when `data.city` is set. Fallback hint "set your city for weather → Farm › Layout" verified accurate — Setup component (subTab `"setup"`, tab label "⚙️ Layout") holds the city picker. Bundle +6.6 KB. |
| 2026-05-11 | **5.2 streak module partial** — old tiny streak corner card replaced with a full-width streak band below the hero header. Bigger 🔥/🌱 icon, 18px day count in `F.head`, supporting line for personal best / celebration / start-your-streak. Last-7-days strip on the right (filled green ✓ when `data.completions[dateKey]` has entries, empty bordered dot otherwise, today gets a subtle `0 0 0 2px C.gp` ring). `last7Days` useMemo keyed on `data.completions`. **Streak Freeze mechanic deferred** to its own commit — adding `freezesAvailable` + `lastFreezeGrant` schema fields with a migration and changing `updateGamify` branching is a separate concern that deserves its own design pass (when does a freeze grant? auto-consume or confirm? indicator visibility when zero?). Bundle net change <1 KB — added band roughly balanced by removing the corner card. |
| 2026-05-11 | **5.6 bento tiles shipped** — info-boxes row below the streak band switched from equal-width `.g5` to a mixed-size `.bento` grid. New CSS classes `.bento` + `.bento-wide` with three breakpoints (4-col on desktop / 2-col on tablet ≤767px / 1-col on phone ≤479px). Today's Work, Growing, and Money tagged `bento-wide` (span 2); Crops + Animals stay 1-col. Today's Work gets visual emphasis as the daily focal point. No content density change to the wide tiles — that's a follow-up if the empty lateral space feels off in real use. |
| 2026-05-11 | **5.5 Suggestions section shipped** — new section above Recent Activity on TodayScreen that surfaces data-state-derived CTAs (not log-history-derived). v1 has one signal: when Pantry holds fresh harvest items (source `harvest`, addedDate within 7 days, unit `kg`, qty>0), shows "{Nkg} fresh {names} won't last forever" with a "How to preserve →" button routing to Manuals. **Deviated from plan literal wording** — the plan example "Yesterday you harvested 4kg mint — preserve some?" implies log-history parsing; data-state is the stronger signal (CTA fires only while produce is still in Pantry, regardless of log pruning) and avoids fragile parsing of user-facing log copy. Section is the only place in the app that bridges Pantry items to the 13 preservation methods in Manuals — closes a real discoverability gap. Architecture extensible — more suggestion patterns can be added by pushing entries into the `suggestions` array. |
| 2026-05-15 | **Phase 6 complete (sans 6.3.3).** All 11 open items across Seasonal / Pantry / Financials / Manuals / AI FAB shipped under commits `090d98d` (6.4.1–6.4.3 + 6.5.1–6.5.3 + 6.6.1–6.6.3 + 6.7.1–6.7.2) and the follow-up AI FAB commit. **6.3.3 deferred to post-Supabase** — photos in localStorage at 5MB quota is a non-starter for casual phone photography; revisit when Supabase Storage is available so photos can be real blob URLs not key-value entries. |
| 2026-05-15 | **6.8 design choices.** (a) Icon: composed Lucide `MessageCircle` + `Leaf` instead of single icon — "ask the farm assistant" reads more clearly than a generic chat bubble or a generic leaf. Composition stays inside the 56px FAB with no overflow. (b) Pulse signal: counts harvest-ready plots (elapsed days ≥ `crop.days`) rather than wiring through `buildTaskQueue` — same intent as `pri:0` tasks but computed locally in the FAB so the component stays cheap and dependency-light. Trade-off: misses urgent step-tasks and animal-care due dates. Acceptable for v1; can be widened later if real usage shows the badge under-firing. (c) Mobile reposition: per Dervis's instruction, kept visible everywhere including inside WalkOverlay — no hide rules. The FAB now sits at `calc(72px + env(safe-area-inset-bottom))` on mobile, clear of the 56px BottomNav. |
| 2026-05-15 | **Phase 7 onboarding shipped.** All 4 screens live in `src/features/onboarding/Onboarding.jsx`. Triggered on first visit AND after data reset. "Drag one zone" simplified to a card-picker (lower scope, higher completion rate). Skip path writes WE defaults with `setupDone:false`. Bundle `index-BLqbc0kD.js`, all 6 markers verified live. |
| 2026-05-15 | **Phase 8 motion polish shipped (originally).** 8.1 FLIP via hand-rolled `useFlip`. 8.2 spring physics + 8.3 shared-element transitions via `framer-motion` (`layoutId`, `motion.div drag="y"`, useMotionValue, useTransform). 8.4 BadgeCelebration overlay with confetti + vibration + reduced-motion compliance. 8.5 living farm map (glisten, urgent pulse, day/night tint, growth bars). 8.6 reduce-motion CSS extended. 8.7 skeletons via existing `shimmer` keyframe. |
| 2026-05-19 | **framer-motion removed (commit `1db4515`).** Tradeoff call: the spring-physics drag-to-dismiss and shared-element transitions were nice, but framer-motion adds ~30 KB gz to the bundle and its layout engine had perceptible jank on iOS Safari touch-cancel under the WalkOverlay. Reverted 8.2 to native CSS transitions (still uses the `useSwipe` touch hook). Dropped 8.3 entirely — no native CSS equivalent for cross-component shared-element transitions today. Real-device tested 2026-05-21 by Dervis — acceptable. |
| 2026-05-15–05-21 | **Phase 6.10 — unplanned Living Farm Map polish loop.** Six days of iteration after the original Phase 6.9 PNG-based map landed. Redesigned as "minimal living planner" (`66bec76`), added stage-aware crop patches + progress gauges (`dd54b24`, `9834419`), removed crop-patch drag/resize in favor of auto-stacking (`2fec54b`), switched editor to pointer events (`a175943`), unified Dashboard + Walk on the same `LivingFarmMap` component (`a5fdb12`), then 8 rapid fixes on WalkOverlay fit/portal (`f472cb3` → `420eba1`). All real-device tested 2026-05-21. Scope was driven by what didn't work on mobile, not by a written spec. |
| 2026-05-20 | **DESIGN_PLAN.md accidentally deleted in commit `bf92dee` "Clean up codecheck findings".** An automated codecheck tool flagged the file as unused doc and applied a too-aggressive cleanup. Caught and restored on 2026-05-21. |
| 2026-05-21 | **DESIGN_PLAN.md recovery + audit pass.** Restored from `bf92dee^:DESIGN_PLAN.md`. Audited against shipped code: corrected **8.2 + 8.3** (framer-motion removed, see 2026-05-19 entry above), **6.9.6** (4 of 10 zone tiles actually ship, downgraded `[x]` → `[~]`), and added a new **6.10** section capturing the post-Phase-6 Living Farm Map polish work. Execution-order table updated. **Phase 9 + Phase 10 deferred** in favor of moving to the architecture-side Phase 5 (Supabase auth + cloud backup) — accounts + cloud + monetization are higher-priority than microcopy and a11y polish before launch. |
| 2026-06-10 | **6.11 Map v3 + FarmIcon shipped.** Mobile-design pass on map + dashboard per Dervis's brief. 25-icon custom SVG set (`FarmIcon.jsx`) kills the colored-dot emoji problem (Radish 🔴, Beetroot 🟣, etc.) across 12 display surfaces; CropStagePatch rebuilt icon-first (white chip + progress ring + sand→green→amber stage tints) replacing the 4-label texture-blob design; zone pills now show fill % and animal head counts on the map itself; harvest pulse softened red→amber; three real bugs fixed (stray `)` text node on Today, lying crop-color legends ×2, dead ZoneOverlay animal list). Verified: clean build, bundle marker grep (`data-crop-patch`/`data-zone-pill`/`data-farm-icon` present, old dark-blob rgba absent). Not verified: visual feel on a real device — the Supabase auth wall blocks an automated screenshot pass, so 6.11.6 stays open for Dervis's eyes. |
| 2026-06-10 | **6.11 follow-up: task-row icons.** TaskQueue events/timeline/Done-Today builders + day-detail pass-through now carry `cropName`/`speciesType`; FarmIcon rendered in TaskQueue rows (22px), day-detail rows (20px), calendar-cell minis (11px), and the Today pipeline rows (14px). Kills the last visible colored-dot surfaces (task lists were the most-seen rows in the app). Verified: build clean, bundle carries cropName ×19 / speciesType ×23. |
| 2026-07-02 | **Grove decision.** Two mockup rounds: paint (Daylight/Nightfarm/Almanac) then structure (Grove/Walk/Console). Dervis picked **Grove — the map is the app** with the **Daylight** paint system. Phase 11 added; Notion × Planta lane superseded. Dark mode kept as toggle (forest-charcoal). |
| 2026-07-02 | **11.1 tokens shipped.** In-place re-value, zero renames; Plus Jakarta Sans removed (all-Inter); C.grd/C.grdHero var-based; theme-color split light/dark; manifest + meta descriptions de-Mediterraneanized. |
| 2026-07-02 | **11.2–11.6 shipped: Grove is the app.** New src/features/grove/: sceneMath.js (iso projector, min-height ratio), GroveScene.jsx (SVG plates w/ side faces + per-type detailing, hut buildings w/ long-axis ridge + door, orchard trees w/ ready rings, crop sprites via the unified row-pack at 4 growth sizes, animal chips, front-edge zone tags, bobbing task markers from buildTaskQueue, hour tint, ZoneOverlay wiring), GroveHome.jsx (weather+streak chips, scene w/ tasksByZone, legend, inline task sheet w/ walk CTA + Attention/Routine + step-toggle-aware completion). Nav flip: home=Farm (scene), dock 4 items, Layout moved to More, TodayScreen unrouted (file kept). Sandbox screenshot-verified on 8-zone fixture, zero console errors. Deferred: fixed-position sheet w/ drag physics, roads on scene, activity feed relocation, 11.7 deep per-screen cleanup. |
| 2026-07-02 | **11.6b + 11.7/11.8 close-out.** Mobile home sheet is now FIXED with two snap states (peek 122px / 70vh full) — drag or tap the grab region; z 300 under nav/overlays; AI FAB lifts to clear it on home (new lift prop). Desktop home column capped at 960px so the sheet sits above the fold. 11.7 verified LIVE via logged-in walk of Tasks/Crops/Animals/Pantry/Financials/Manuals in dark + home in light — all screens coherent on the Daylight/forest tokens, no stragglers. 11.8 shipped scope: marker bob, ready halo/ring pulse, sheet snap transition, hour tint, reduced-motion overrides; stage-change grow transition dropped as non-essential. |
| 2026-07-02 | **11.9 shipped.** GroveZoneCard (z 5000, portal, spring-in card + staggered rows + scaleX progress bars) replaces ZoneOverlay on the Grove scene only. Idle motion: .grove-sway on sprite icons + .grove-tree-sway (transform-box:fill-box) on tree groups, negative animation-delays for phase variety, .grove-pop mount animation. Verified in 420px harness: veg card (Tomatoes 100% Ready amber bar, Carrots 53%, Lettuce 13%) + orchard card, zero console errors. |
| 2026-07-02 | **11.10 shipped.** (1) Real-area stage patches on the scene: each plot renders as an iso polygon covering its true fraction of the zone (same row-pack), filled by stage — planted soil #c9a97b / growing #8cbd71 / ready gold #e3b45c with amber stroke; proportional inset so slivers survive. (2) Zone-card actions per crop: "Show more →" navigates to Crops; "🧺 Harvest" (ready only) runs the canonical WalkOverlay flow — plot→harvested, expectedYieldKg→pantry Fresh Produce, log entry, plot-{id}-harvest task cleared. DOM-asserted in harness: 5 patches at 3 stages w/ widths 124–176vb, harvest removes patch + row + task (12→11), zero errors. |
